[build]
builder = "DOCKERFILE"
dockerfilePath = "backend-ts/Dockerfile"
dockerfileContext = "backend-ts"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
